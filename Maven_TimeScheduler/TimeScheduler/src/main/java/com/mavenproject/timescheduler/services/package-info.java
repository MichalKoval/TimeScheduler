/**
 * Balicek obsahuje triedu pre generovanie dat, pre nasledne otestovanie.
 * a taktiez obsahuje TimerTask triedu WakeUpForNotification, ktora v urcitych prednastavenych casovych intervaloch zabezpeci zobrazenie aktivity.
 * Trieda 'WakeUpForNotification' je pouzivana triedou 'NotificationHandler', ktory ju pouziva ako vlakno v casovaci Timer.
 */
package com.mavenproject.timescheduler.services;
